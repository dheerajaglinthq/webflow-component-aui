import * as React from "react";
import * as Types from "./types";

declare function SalaryDetailinfo(props: {
  as?: React.ElementType;
  max?: React.ReactNode;
  med?: React.ReactNode;
  min?: React.ReactNode;
  source?: React.ReactNode;
  currency?: React.ReactNode;
}): React.JSX.Element;
